i have an oracle database. there are two types of users, user and admin. user gets a different page on logging in than admin. The user page and the admin page both have the option to log out in the top right of the page as a button. after clicking this, the html site loads the log in page. When a user or admin inputs their user name and their password, they are taken to their respective pages. An error is displayed if the password is wrong or the user does not exist in the database for users. There is also a button on the log in page to go to a form to create a user account. admin accounts can not be made with this form. if the form is filled in successfully, the user is added to the database and they go to the user page with their data. if the creation of a new user fails, an error is displayed and they stay on the form creation page. On the user creation page, instead of a button to log out, there is a button to return to the log in page in the top right. create the template html files for the log in page, user creation page, user page and admin page. include a css style sheet the connects to all of them.


i have flask. this is my user database code: CREATE TABLE PROJECTUSERREV
( 
user_id NUMBER,
first_name VARCHAR2(255) NOT NULL, 
last_name VARCHAR2(255) NOT NULL, 
password_hash VARCHAR2(255) NOT NULL,  
role VARCHAR2(20) CHECK(role IN('user','admin'))
CONSTRAINT user_pk_rev PRIMARY KEY (user_id)
); i want to check for role from this database and load the correct page. i want the password to be encrypted and compared to the encrypted password in the database for log in. there is a separate table for user emails, the code for that is: CREATE TABLE PROJECTEMAILREV
( 
user_id NUMBER, 
email VARCHAR2(255) NOT NULL,
CONSTRAINT fk_useremail_rev FOREIGN KEY (user_id) REFERENCES ProjectUserRev(user_id),
CONSTRAINT pk_email_rev PRIMARY KEY (user_id, email)
); there is a separate table for social media, the code for that is: CREATE TABLE PROJECTSOCIALMEDIAREV
( 
user_id NUMBER, 
socialmedia VARCHAR2(255) NOT NULL,
CONSTRAINT fk_usersocialmedia_rev FOREIGN KEY (user_id) REFERENCES ProjectUserRev(user_id),
CONSTRAINT pk_socialmedia_rev PRIMARY KEY (user_id, socialmedia)
); I want to load the correct user data on log in using the first and last name and the email addresses, tell me the efficiency of this and give me the code to connect the log in with the database. on log in, the user or admin data should be displayed. also, add the code that connects the user creation to the database and verifies that all required fields are filled correctly.


what does the file structure look like?


the user creation page needs both a first and a last name. role is automatically assigned as 'user'. There needs to be multiple slots for email and social media. a user id number is generated by concatenating the ascii values for the first name char with the ascii values for the first email entered. If either of these fields is blank, a user can not be made. the sql for the database is already built. i want to insert values into it.

where do scripts go in an html file?

i am getting a file not found URL error on the login page

i need instructions for running oracle database with developer

the ,exe is in the downloads folder, and i can't find my old tables

i see the tables, there is a lot of default junk in there with them

should they be in the same folder? or should oracle db not be in downloads?

OracleXETNSListener is not running

i have a home version of oracle running: OracleOraDB21Home1TNSListener

fix this: app = Flask(__name__) app.secret_key = "your_secret_key" # Oracle DB connection dsn = oracledb.makedsn("localhost", 1521, service_name="XEPDB1") # remove before committing or turning in def get_connection(): return oracledb.connect(user="system", password="password", dsn=dsn)

when i click on create user on the login page, i get: not found URL error

give me the flask links for all the html pages so i dan update

i tried to make a new user with the ui, but it failed

i see nothing, and the page is not found

This site can’t be reached
127.0.0.1 refused to connect.

bash says: REQUEST: POST /create_user
POST HIT Create
ImmutableMultiDict([('first_name', 'test_ui'), ('last_name', 'flask'), ('password', 'pass4569'), ('emails[]', 'that_email@gmail.com'), ('socials[]', 'test@facebook.com')])
DB ERROR: DPY-4003: value cannot be represented as an Oracle number

several hanging errors later:

i'm sure it's that last bit of code added, how do i put user_id NUMBER GENERATED BY DEFAULT AS IDENTITY into the table?


you created the crash by suggesting multiple contradictory things

still hanging and loading forever. nothing in terminal. restarting everything.

